#!/usr/bin/node

// Define an array of strings
const masseges = ['C is fun', 'Python is cool', 'JavaSricpt is amazing'];
// Loop through the elements of the masseges array
for (let i = 0; i < masseges.length; i++) {
  // Print out each massege in the array
  console.log(masseges[i]);
}
